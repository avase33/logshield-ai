global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: logshield-workers
    metrics_path: /metrics
    static_configs:
      - targets: ["logshield:8000"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
